import React from 'react';
import ReactDOM from 'react-dom'; //DOM é arvore de elementos do html
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
